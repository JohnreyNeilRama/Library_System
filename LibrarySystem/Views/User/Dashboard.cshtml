@model LibrarySystem.Controllers.UserInfoViewModel

@{
    ViewData["Title"] = "User Dashboard";
    Layout = "_UserLayout";
}

<div class="dashboard-container">
    <div class="welcome-banner">
        <div>
            <h4 class="fw-bold mb-1">Welcome back, @Model.FullName!</h4>
            <p class="mb-0 opacity-75">Here is your library overview</p>
        </div>
    </div>

    <div class="stats-grid">
        <div class="stat-card">
            <span class="stat-num">5</span>
            <span class="stat-lbl">Borrowed</span>
        </div>
        <div class="stat-card">
            <span class="stat-num">12</span>
            <span class="stat-lbl">Returned</span>
        </div>
        <div class="stat-card">
            <span class="stat-num">2</span>
            <span class="stat-lbl">Pending</span>
        </div>
        <div class="stat-card">
            <span class="stat-num">3</span>
            <span class="stat-lbl">Reserved</span>
        </div>
    </div>

    <div class="content-sections">
        <!-- LEFT SIDE: User Info Card -->
        <div class="main-section">
            <div class="profile-card user-info-card">
                <div class="section-title">
                    <h5 class="fw-bold mb-0">Personal Information</h5>
                    <a asp-controller="User" asp-action="Profile" class="btn btn-sm btn-primary">Edit</a>
                </div>

                <!-- Profile Picture Only (no name) -->
                <div class="profile-preview">
                    <div class="profile-preview-avatar">
                        @if (!string.IsNullOrEmpty(Model.ProfilePictureUrl))
                        {
                            <img src="@Model.ProfilePictureUrl" alt="Profile" class="profile-avatar-img">
                        }
                        else
                        {
                            <div class="profile-avatar-placeholder">
                                @(Model.FullName?.Split(' ')[0].Substring(0, 1) ?? "U")
                            </div>
                        }
                    </div>
                </div>

                <!-- User Details (including Full Name) -->
                <div class="user-info-list">
                    <div class="info-item">
                        <span class="info-label">Full Name</span>
                        <span class="info-value">@Model.FullName</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">ID Number</span>
                        <span class="info-value">@Model.IdNumber</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Course</span>
                        <span class="info-value">@Model.Course</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Year Level</span>
                        <span class="info-value">@Model.YearLevel</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Email</span>
                        <span class="info-value">@Model.Email</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- RIGHT SIDE: Activity + Borrowed + Reserved -->
        <div class="side-section">
            <div class="activity-card">
                <h6 class="fw-bold mb-2">Recent Activity</h6>
                <div class="activity-items">
                    <div class="activity-item">
                        <span class="activity-icon">📗</span>
                        <div>
                            <span>Returned "The Hobbit"</span>
                            <small class="text-muted">2 days ago</small>
                        </div>
                    </div>
                    <div class="activity-item">
                        <span class="activity-icon">📖</span>
                        <div>
                            <span>Borrowed "Pride and Prejudice"</span>
                            <small class="text-muted">5 days ago</small>
                        </div>
                    </div>
                    <div class="activity-item">
                        <span class="activity-icon">📚</span>
                        <div>
                            <span>Reserved "The Catcher in the Rye"</span>
                            <small class="text-muted">1 week ago</small>
                        </div>
                    </div>
                </div>
            </div>

            <div class="section-card mt-3">
                <div class="section-title">
                    <h5 class="fw-bold">Currently Borrowed</h5>
                    <a href="#" class="btn btn-sm btn-outline-primary">View All</a>
                </div>
                <div class="books-list">
                    <div class="book-row">
                        <div class="book-cover" style="background: linear-gradient(135deg, #667eea, #764ba2);"></div>
                        <div class="book-info">
                            <h6 class="mb-0">The Great Gatsby</h6>
                            <small class="text-muted">Due: Apr 29, 2026</small>
                        </div>
                        <span class="badge-status warning">Due Soon</span>
                    </div>
                    <div class="book-row">
                        <div class="book-cover" style="background: linear-gradient(135deg, #f093fb, #f5576c);"></div>
                        <div class="book-info">
                            <h6 class="mb-0">1984</h6>
                            <small class="text-muted">Due: Apr 24, 2026</small>
                        </div>
                        <span class="badge-status success">On Time</span>
                    </div>
                    <div class="book-row">
                        <div class="book-cover" style="background: linear-gradient(135deg, #4facfe, #00f2fe);"></div>
                        <div class="book-info">
                            <h6 class="mb-0">To Kill a Mockingbird</h6>
                            <small class="text-muted">Due: Apr 22, 2026</small>
                        </div>
                        <span class="badge-status danger">Overdue</span>
                    </div>
                </div>
            </div>

            <div class="section-card mt-3">
                <div class="section-title">
                    <h5 class="fw-bold">Reserved Books</h5>
                    <a href="#" class="btn btn-sm btn-outline-primary">View All</a>
                </div>
                <div class="reserved-list">
                    <div class="reserved-row">
                        <div class="book-cover" style="background: linear-gradient(135deg, #fa709a, #fee140);"></div>
                        <div class="book-info">
                            <h6 class="mb-0">The Catcher in the Rye</h6>
                            <small class="text-muted">Ready for Pickup</small>
                        </div>
                        <button class="btn btn-sm btn-success">Pick Up</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>